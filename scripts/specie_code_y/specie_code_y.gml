// return specie code "y code" as integer
function specie_code_y(genome){
	// 26/4/21 -- should be always zero, to be deprecated
	//return int64(string_char_at(genome[GEN.SPECIE_CODE], 5));
	return 0;
}
	
